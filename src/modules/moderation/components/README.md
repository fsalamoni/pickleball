# `moderation/components/` — componentes (PLANEJADO)

| Componente | Onde é usado |
|---|---|
| `ReportDialog.jsx` | **compartilhado**: post, comentário, anúncio, avaliação, vendedor, usuário |
| `BlockButton.jsx` | perfil, menu do post, página do vendedor |
| `MuteMenuItem.jsx` | menu `⋯` do post |
| `StrikeBanner.jsx` | topo do feed e do painel do vendedor quando há strike ativo |
| `RateLimitNotice.jsx` | composer e wizard de anúncio, explicando o limite e quando libera |

A fila do admin fica em `src/v2/components/admin/AdminModerationQueue.jsx`.
